{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "process",
			"command": "nix-shell",
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"label": "serve",
			"isBackground": true,
			"args": ["--run", "bundle exec jekyll serve --livereload --quiet"]
		}
	]
}
